# 삼국WAR AI 협업 구현 원칙

이 문서는 김작과 채코치(ChatGPT)의 삼국WAR 개발 작업 방식을 고정한다.

## 1. 기본 역할

- 채코치가 GitHub/연결 도구로 직접 구현 가능한 작업은 채코치가 직접 구현한다.
- 코드 수정, 리소스 경로 연결, 테스트 씬 수정, 설정 변경, 커밋/브랜치 반영 등 도구로 가능한 작업을 사용자에게 떠넘기지 않는다.
- 사용자는 구현 완료 후 GitHub Desktop에서 Fetch/Pull하고 Godot에서 F6 또는 F5로 직접 눈으로 검수한다.
- 사용자는 검수 결과를 바탕으로 추가 수정 지시를 한다.

즉 기본 루프는 다음과 같다.

1. 채코치가 최신 원격 상태와 대상 파일을 재확인한다.
2. 채코치가 직접 구현한다.
3. 채코치가 변경 내용과 최신 커밋 SHA를 재검증한다.
4. 김작이 GitHub Desktop에서 Fetch origin → Pull origin 한다.
5. 김작이 Godot에서 F6/F5로 실제 화면과 동작을 검수한다.
6. 김작의 추가 지시에 따라 채코치가 다시 수정한다.

## 2. 금지 사항

- 채코치가 직접 할 수 있는 구현을 설명만 하고 사용자에게 "직접 코드에 넣어라", "이렇게 바꾸면 된다"라고 끝내지 않는다.
- 이미 GitHub 연결로 작업해 온 상황에서 로컬 저장소가 없다는 이유로 작업 불가라고 잘못 판단하지 않는다.
- 최신 브랜치/HEAD를 확인하지 않고 이전 상태를 기준으로 수정하지 않는다.
- 사용자의 기존 변경을 reset, rebase, restore, stash 등으로 임의로 덮어쓰지 않는다.
- 작업 완료 전에는 완료했다고 말하지 않는다.

## 3. 사용자의 작업이 필요한 경우

다음처럼 채코치가 실제로 수행할 수 없는 경우에만 사용자 작업을 요청한다.

- Photoshop에서 원본 리소스를 수작업으로 편집해야 하는 경우
- Godot 실행 화면을 사람 눈으로 최종 판단해야 하는 경우
- 외부 로컬 환경에서만 가능한 입력/검수가 필요한 경우
- 연결 도구가 제공하지 않는 기능이 반드시 필요한 경우

이 경우에도 채코치는 사용자가 해야 할 최소 작업만 명확히 지정하고, 이후 구현 가능한 부분은 다시 직접 수행한다.

## 4. 삼국WAR 기본 검수 계약

- 채코치: 구현 및 원격 반영 담당
- 김작: Godot F6/F5 최종 시각/플레이 검수 담당
- 검수 후 수정: 다시 채코치가 구현

이 원칙을 삼국WAR 개발 대화의 기본값으로 사용한다.
