# UI Formatter

Purpose:
- v0.71 refactor destination for UI formatter and summary helpers extracted from `scripts/worldmap_test.gd`.

Allowed later:
- Pure text formatters.
- Bullet builders.
- Tooltip and summary builders.
- Small, verified extraction steps only.

Not allowed yet:
- Moving UI node mutation early.
- Changing scene node paths or script paths.
- Changing UI layout behavior.
- Changing gameplay formulas or schemas.

Current status:
- Placeholder only.
- No runtime script moved here in v0.71-03.
